# Чтобы отключить подписку на офиц. обновления в proxmox

    - советуют в файле /etc/apt/sources.list.d/pve-enterprise.list звкомментировать строку #deb https://enterprise.proxmox.com stretch pve-enterprise, но я не нашел такого файла и нашел в указанной папке файл 

```bash
root@pve:~# ls -al /etc/apt/sources.list.d
total 20
drwxr-xr-x 2 root root 4096 Jan 11 13:58 .
drwxr-xr-x 9 root root 4096 Jan 10 19:40 ..
-rw-r--r-- 1 root root  163 Aug  5 13:42 ceph.sources
-rw-r--r-- 1 root root  365 Aug  5 13:42 debian.sources
-rw-r--r-- 1 root root  160 Aug  5 13:36 pve-enterprise.sources
```
в котором такое содерхимое ->
```text
Types: deb
URIs: https://enterprise.proxmox.com/debian/pve
Suites: trixie
Components: pve-enterprise
Signed-By: /usr/share/keyrings/proxmox-archive-keyring.gpg
```

    - и теперь не ясно, что корректировать?

```text
Совет про файл pve-enterprise.list устарел — начиная с Proxmox VE 7 для управления репозиториями используется современный формат deb822, который вы и обнаружили.
```

## Для отключения коммерческого репозитория и добавления бесплатного можно действовать двумя способами.

### ✏️ Способ 1: Редактирование файла (Быстрый)

    Вам нужно открыть файл /etc/apt/sources.list.d/pve-enterprise.sources и добавить строку Enabled: no к существующему блоку настроек

```bash
nano /etc/apt/sources.list.d/pve-enterprise.sources
```

    После добавления строка файл должен выглядеть так:
```text
Types: deb
URIs: https://enterprise.proxmox.com/debian/pve
Suites: trixie
Components: pve-enterprise
Signed-By: /usr/share/keyrings/proxmox-archive-keyring.gpg
Enabled: no
```

    Сохраните изменения (Ctrl+O, Enter), закройте редактор (Ctrl+X) и выполните команду apt update, чтобы обновить списки пакетов.

### - 🖱️ Способ 2: Через веб-интерфейс (Рекомендуемый)

    Управление репозиториями через GUI — самый простой и надежный способ

```text

    Войдите в веб-интерфейс Proxmox VE.

    Выберите ваш узел pve в левом дереве.

    Перейдите в раздел Updates (Обновления) и выберите вкладку Repositories (Репозитории)

    В списке найдите репозиторий с названием pve-enterprise и нажмите кнопку Disable (Отключить)

    Чтобы добавить бесплатный репозиторий, нажмите Add (Добавить) и в выпадающем списке выберите No-Subscription

        . Затем нажмите Add для подтверждения.
```

    После отключения репозитория сообщение о подписке исчезнет автоматически, а система будет получать обновления из бесплатного репозитория.

    Для надежности после настройки выполните в терминале:
    
```bash
apt update && apt upgrade, чтобы установить все доступные обновления
```

#### Получил ошибку? -> ```root@pve:~# apt update
```text
Hit:1 http://security.debian.org/debian-security trixie-security InRelease
Hit:2 http://deb.debian.org/debian trixie InRelease            
Get:3 http://deb.debian.org/debian trixie-updates InRelease [47.3 kB]
Err:4 https://enterprise.proxmox.com/debian/ceph-squid trixie InRelease    
  401  Unauthorized [IP: 45.84.67.184 443]
Error: Failed to fetch https://enterprise.proxmox.com/debian/ceph-squid/dists/trixie/InRelease  401  Unauthorized [IP: 45.84.67.184 443]
Error: The repository 'https://enterprise.proxmox.com/debian/ceph-squid trixie InRelease' is not signed.
Notice:
```

    Ошибка означает, что система пытается обновить пакеты CEPH из коммерческого репозитория enterprise.proxmox.com, для которого у вас нет активной подписки.
    Это нормально.
    Вам нужно отключить репозиторий CEPH аналогично тому, как вы отключили pve-enterprise.

### 🔧 Как исправить

    Найдите и отключите репозиторий CEPH.
    Посмотрите файл /etc/apt/sources.list.d/ceph.sources:
```bash

cat /etc/apt/sources.list.d/ceph.sources
```

    Скорее всего, в нём будет блок, похожий на pve-enterprise.sources, указывающий на https://enterprise.proxmox.com/debian/ceph-squid.
    Откройте этот файл для редактирования:
```bash

nano /etc/apt/sources.list.d/ceph.sources
```

    Найдите блок с репозиторием enterprise.proxmox.com и добавьте в него строку Enabled: no (как вы делали ранее):
```text
Types: deb
URIs: https://enterprise.proxmox.com/debian/ceph-squid
Suites: trixie
Components: main
Signed-By: /usr/share/keyrings/proxmox-archive-keyring.gpg
Enabled: no
```

    Сохраните файл (Ctrl+O, Enter, Ctrl+X).

    Проверьте, что репозиторий отключён. Для этого снова выполните:
```bash
apt update
```

    Теперь ошибка "401 Unauthorized" должна исчезнуть. Вы увидите только обновления из репозиториев Debian и (если вы его добавили) бесплатного репозитория Proxmox.

### 💡 Дополнительный важный шаг

    Если у вас не установлен сервис CEPH и вы не планируете его использовать, вы можете полностью удалить файл ceph.sources, чтобы избежать подобных проблем в будущем.
```bash

rm /etc/apt/sources.list.d/ceph.sources
```

#### После удаления файла выполните apt update для проверки. Если всё сделано верно, обновление пройдёт без ошибок.

## 🛠️ Универсальное решение через веб-интерфейс

Если вам неудобно редактировать файлы в терминале, вы можете выполнить ту же операцию через веб-интерфейс Proxmox VE:

    Зайдите в интерфейс, выберите ваш узел pve.

    Перейдите в Updates > Repositories.

    Найдите в списке репозиторий, связанный с ceph или enterprise.proxmox.com/debian/ceph-squid.

    Нажмите на него и выберите Disable (Отключить).

После этих действий ошибка исчезнет, и система будет обновляться только из доступных бесплатных источников.
